# Rate limit set high so the shared Spring context is not depleted by the
# 100+ MockMvc requests made across all integration test classes.
# Unit tests (RateLimitFilterTest) construct the filter directly with capacity=3
# and are unaffected by this property.
scim.rate-limit.capacity=10000
scim.rate-limit.refill-per-minute=10000
